-- test is a number, so this script doesn't compile because you can't set a string as a number
test = ""

local test2:boolean = false

-- test2 is annotated as a number, so the script doesn't compile.
test2 = 10